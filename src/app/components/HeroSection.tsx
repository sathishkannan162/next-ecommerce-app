import type { ReactElement } from "react"

export interface HeroSectionProps {
  
}

export default function HeroSection(props: HeroSectionProps): ReactElement {
  return (
      <div className="text-orange-700 w-full h-72 bg-slate-100 flex justify-center items-center">HeroSection</div>
  )
}
